/**
  ******************************************************************************
  * @file    config.h
  * @brief   Центральная конфигурация прошивки — узел ОХЛАЖДЕНИЯ (controller-cool).
  *
  *          Узел управляет двумя вентиляторами SPAL VA26-AP50/C-44A (12 В,
  *          ~130 Вт каждый), подключёнными к силовым выходам L1 (драйвер DRV1)
  *          и L2 (драйвер DRV2). Вентиляторы плавно запускаются до половины
  *          максимальных оборотов по команде ведущего узла (device_id 0x01), который транслирует по CAN
  *          состояние своих дискретных входов; используется ТОТ ЖЕ вход
  *          компаратора comp[6] (deadman), который у ведущего разрешает выдачу
  *          оборотов на VESC (см. ControlFSM_Update в controller-master).
  *
  *          Плата и распиновка — те же, что у controller-front; роль узла
  *          задаётся составом сборки, а не флагом.
  ******************************************************************************
  */

#ifndef __CONFIG_H
#define __CONFIG_H

#ifdef __cplusplus
extern "C" {
#endif

#include <stdbool.h>
#include <stdint.h>

/* ===== Идентификация узла на шине CAN ====================================== */
extern uint8_t device_id;            /* этот узел: 0x05                        */
#define MASTER_DEVICE_ID 0x01        /* ведущий узел — источник команд         */

/* ===== Команда пуска вентиляторов ========================================== */
/* Ведущий периодически шлёт кадр BASE_COMP со снимком своих восьми дискретных
 * входов. Байт с индексом COMP_DEADMAN — тот самый контакт компаратора, по
 * которому ведущий разрешает работу мотора складывания и отправку оборотов на
 * VESC: при нулевом входе автомат ведущего уходит в DRIVE_DISABLED и erpm = 0.
 * Вентиляторы включаются по этому же признаку — охлаждение работает ровно
 * тогда, когда машина разрешена к движению.                                  */
#define COMP_DEADMAN  6     /* индекс байта в кадре comp ведущего            */

/* ===== ШИМ вентиляторов (TIM4: CH1/CH2 = DRV1, CH3/CH4 = DRV2) ============= */
/* Частота ШИМ 72 МГц / 1 / 18000 = 4 кГц — минимум потерь в ключах.
 *
 * ПОТЕРИ В КЛЮЧЕ складываются из двух частей:
 *     P_пров   = I²·Rds(on)        — от частоты НЕ зависит;
 *     P_перекл = U·I·t_перекл·f    — растёт линейно с частотой.
 * Переключается только регулируемое плечо (IN_A); второе (IN_B) статично и
 * имеет лишь потери проводимости.
 *
 * Расчёт для худшего ДЛИТЕЛЬНОГО режима — 9 А (порог защиты по среднему току,
 * выше него канал отключается за 2 с), U = 14.4 В на заряде, Rds(on) = 40 мОм
 * в горячем состоянии, время перехода t_перекл = 2 мкс:
 *     P_пров   = 81 · 0.04                    = 3.2 Вт  (частота ни при чём)
 *     P_перекл = 14.4 · 9 · 2e-6 · f
 *         16 кГц -> 4.1 Вт,  итого 7.4 Вт
 *          8 кГц -> 2.1 Вт,  итого 5.3 Вт
 *          4 кГц -> 1.0 Вт,  итого 4.3 Вт    <- выбрано
 * В рабочей точке (2.7 А, 13 В) переключение даёт 0.28 Вт против 1.1 Вт на
 * 16 кГц. Дальше снижать частоту почти бессмысленно: 3.2 Вт проводимости
 * остаются при любой частоте и становятся главной статьёй.
 *
 * Выбор устойчив к незнанию типа ключа: конкретный силовой элемент в
 * документации платы не зафиксирован (топология выводов — своя пара IN/EN на
 * полумост — соответствует классу BTS7960 со slew rate ~10 В/мкс, то есть
 * переход ~1.3..1.5 мкс). На 4 кГц потери переключения остаются заметно ниже
 * потерь проводимости во всём правдоподобном диапазоне t_перекл = 0.5..3 мкс.
 *
 * ЧЕМ ПЛАТИМ — ПУЛЬСАЦИЕЙ ТОКА ЯКОРЯ. Нагрузка индуктивная, размах пульсации
 * dI = U·D·(1−D)/(L·f). При U = 13 В, D = 0.46 и среднем токе ~2.7 А:
 *     L = 300 мкГн -> 2.7 А размаха: ток непрерывный;
 *     L = 200 мкГн -> 4.0 А размаха: ток ходит 0.7..4.7 А, ещё непрерывный;
 *     L = 100 мкГн -> 8.1 А размаха: ток доходит до нуля и РЕВЕРСИРУЕТ.
 * Индуктивность якоря этого вентилятора точно не известна (оценка ~200 мкГн),
 * поэтому 4 кГц — на границе. Реверс не опасен (ток идёт по тому же контуру
 * циркуляции через верхние ключи), но добавляет действующего тока, пульсацию
 * момента и износ щёток; на потери это влияет слабо — при L = 200 мкГн
 * действующий ток растёт с 2.7 до 2.9 А, то есть проводимость +18 % от
 * небольшой величины.
 *
 * ЧТО ПРОВЕРИТЬ НА ЖЕЛЕЗЕ: осциллограмму тока в рабочей точке. Если размах
 * пульсации доходит до нуля и меняет знак — поднять частоту до 8 кГц
 * (PWM_PERIOD = 9000); потери при этом вырастут примерно на 1 Вт на канал.
 *
 * ЦЕНА ПО ШУМУ: 4 кГц — резкий слышимый свист. На гусеничной машине с
 * работающим двигателем и вентилятором Ø280 мм это несущественно.
 *
 * PWM_PERIOD кратен 100 — расчёт скважности в процентах точен, без остатка.
 * ARR = 17999 умещается в 16-битный счётчик TIM4.                           */
#define PWM_PERIOD    18000  /* период ШИМ вентиляторов (отсчёты TIM4)       */

/* ===== Рабочая точка и плавный пуск ======================================= */
/* Задача — 50 % от МАКСИМАЛЬНОЙ СКОРОСТИ ВРАЩЕНИЯ.
 *
 * Тахометра у VA26-AP50/C-44A нет (двухпроводной вентилятор), поэтому обороты
 * задаются разомкнуто, через скважность. Для коллекторного двигателя
 * U = k·w + I·Ra, а момент сопротивления крыльчатки растёт как квадрат
 * оборотов, поэтому на половине оборотов ток падает примерно вчетверо и
 * падение I·Ra почти исчезает: нужное напряжение оказывается чуть НИЖЕ
 * половины питания. Для этого вентилятора (130 Вт, ~10.8 А, Ra ~0.2 Ом)
 * решение уравнения даёт 50 % скважности -> ~54 % оборотов, а ровно 50 %
 * оборотов -> ~46 % скважности. Разброс Ra между экземплярами больше этой
 * разницы, поэтому взято 46 % как расчётная середина.
 *
 * Это единственная точка настройки: если по факту (на слух/тахометром)
 * обороты уйдут от половины, правится только эта константа. Разомкнутое
 * управление зависит и от бортсети — при 14.4 В обороты будут выше, чем при
 * 12.0 В; измерения напряжения на плате нет, компенсировать нечем.          */
#define FAN_DUTY_TARGET       46   /* рабочая скважность, % (~50 % оборотов)  */

/* Плавный пуск: скважность нарастает 0 -> FAN_DUTY_TARGET за FAN_RAMP_UP_MS.
 * Пусковой ток вентилятора этого класса при прямом включении достигает
 * 40..60 А (заторможенный ротор), что мгновенно насытило бы ACS724 (±20 А) и
 * просадило бы бортсеть. Разгон за 3 с удерживает ток в рабочем диапазоне
 * датчика и позволяет защите различать «разгон» и «клин ротора».            */
#define FAN_RAMP_UP_MS      3000   /* время разгона 0 -> рабочая точка, мс   */
#define FAN_RAMP_DOWN_MS    1500   /* время управляемого снижения до 0, мс   */
#define FAN_RAMP_INTERVAL_MS  20   /* период пересчёта скважности, мс        */

/* Разнос пусков вентиляторов: второй стартует позже первого, чтобы пусковые
 * токи двух двигателей не складывались на общем питании и на одном
 * предохранителе.                                                            */
#define FAN_STAGGER_MS       800   /* задержка пуска второго вентилятора, мс */

/* ===== ЛОВУШКА: СНЯТИЕ EN НЕ ГАСИТ НАГРУЗКУ, А ВКЛЮЧАЕТ ЕЁ НА ПОЛНУЮ ======
 * Проверено на железе (DRV_OUTPUT_STAGE.md): при СНЯТИИ разрешения драйвера
 * затвор верхнего P-канального ключа остаётся притянут вниз, ключ открывается
 * ПОЛНОСТЬЮ, выход залипает на V_BAT, а записи в регистры сравнения перестают
 * на что-либо влиять. Причина видна по схеме: делитель R59 22k на V_BAT +
 * R61 33k на GND садит затвор примерно на 0.6·V_BAT, то есть Vgs ~ -5 В.
 *
 * ЭТО ПРЯМО ЗАТРАГИВАЛО ЭТОТ УЗЕЛ. Прежняя прошивка использовала EN как
 * выключатель: поднимала его на время работы и снимала в OFF/FAULT. На этом
 * каскаде такое «выключение» означает вентилятор на ПОЛНОМ ходу — то есть
 * вентиляторы крутились всегда, когда прошивка считала их остановленными, а
 * защита по току, срабатывая, включала неисправный канал вместо того, чтобы
 * его обесточить.
 *
 * ОТСЮДА ПРАВИЛО, которому подчинён весь автомат в cooling.c:
 *   - EN поднимается ОДИН раз при старте, ПОСЛЕ того как значения сравнения
 *     выставлены в «закрыто», и больше не трогается никогда;
 *   - «выключено» — это положение ПЛЕЧ, а не состояние EN;
 *   - состояния «мост обесточен» на этой плате не существует.               */

/* ===== Останов: стекание индукции и механический выбег ==================== */
/* Схема останова (см. cooling.c):
 *   1) скважность плавно снижается до нуля (FAN_RAMP_DOWN_MS);
 *   2) верхнее плечо закрыто, НИЖНЕЕ ОСТАЁТСЯ ОТКРЫТЫМ: ток якоря замыкается
 *      по контуру GND -> VD32/33/34 -> обмотка -> датчик -> нижний ключ -> GND
 *      и коммутируется в него без выброса напряжения;
 *   3) по истечении FAN_DECAY_MS закрывается и НИЖНЕЕ плечо — обмотка
 *      размыкается, выход остаётся высокоомным, и крыльчатка дальше выбегает
 *      СВОБОДНО (см. FAN_COAST_MS).
 *
 * Шаг 3 раньше выполнялся снятием EN — то есть делал ровно противоположное
 * (см. ловушку выше). Закрытие нижнего плеча даёт именно то, что и
 * предполагалось: разомкнутую обмотку.
 *
 * ЭЛЕКТРИЧЕСКАЯ и МЕХАНИЧЕСКАЯ части останова — разные процессы, и путать их
 * нельзя:
 *
 *   - электрическая: постоянная времени якоря L/R при L ≈ 200 мкГн и
 *     R ≈ 0.2 Ом — около 1 мс. 30 мс это тридцать постоянных времени, с
 *     большим запасом;
 *
 *   - механическая: крыльчатка Ø280 мм выбегает 10..20 с. И всё это время
 *     двигатель остаётся ГЕНЕРАТОРОМ.
 *
 * Отсюда FAN_DECAY_MS сделан коротким. Замкнутая на контур гашения обмотка —
 * это не «стекание», а ДИНАМИЧЕСКОЕ ТОРМОЖЕНИЕ: ток в ней не спадает, а
 * устанавливается на ЭДС/Ra (при половине оборотов это ~30 А) и держится,
 * пока крутится ротор. Держать такое замыкание сотни миллисекунд — значит
 * без нужды греть ключи кинетической энергией крыльчатки. Тридцати
 * миллисекунд хватает, чтобы ток якоря перешёл в контур гашения, и мало,
 * чтобы затормозить ротор (теряется ~10 % оборотов).
 *
 * После закрытия обоих плеч обмотка разомкнута: выходы висят между шинами
 * через банки обратных диодов, ЭДС генератора (максимум ~12 В) ниже питания,
 * поэтому ни один диод не открывается — тока нет, выбег свободный.          */
#define FAN_DECAY_MS          30   /* выдержка гашения перед размыканием     */

/* --- Модель свободного выбега крыльчатки ---------------------------------
 * Нужна для ПОВТОРНОГО ПУСКА В ВЫБЕГЕ. Подхватывать вращающийся вентилятор
 * нулевой скважностью нельзя: при duty = 0 верхнее плечо закрыто, а нижнее
 * открыто, то есть обмотка замкнута на контур гашения — открыть нижний ключ
 * под вращающейся крыльчаткой значит мгновенно получить ток ЭДС/Ra в десятки
 * ампер (короткое замыкание генератора). Поэтому перед подключением обмотки
 * мост выставляется на скважность, ЭКВИВАЛЕНТНУЮ текущим оборотам, и только
 * потом начинается разгон.
 *
 * Оборотов измерять нечем (вентилятор двухпроводной, тахометра нет), поэтому
 * они оцениваются по времени. Момент сопротивления воздуха растёт как квадрат
 * оборотов, значит свободный выбег гиперболический, а не экспоненциальный:
 *     w(t) = w0 / (1 + t/tau)
 * При tau = 2.5 с это ~29 % оборотов через 6 с и ~11 % через 20 с — совпадает
 * с наблюдаемым выбегом 10..20 с до полной остановки.
 *
 * Точность модели здесь некритична: ошибка оценки dD даёт ток dD·U/Ra, и даже
 * промах в 30 % рабочей точки — это меньше 10 А, вдвое ниже порога пиковой
 * защиты. Важно не попасть в замыкание генератора, а это модель исключает. */
#define FAN_COAST_MS       20000   /* после этого крыльчатка считается стоящей */
#define FAN_COAST_TAU_MS    2500   /* постоянная гиперболического выбега      */

/* ===== Защита от быстрого пере-включения ================================== */
/* Три независимых механизма против цикла «выключил — тут же включил», от
 * которого выгорают ключи моста.
 *
 * 1. АНТИДРЕБЕЗГ КОМАНДЫ. Вход deadman на ведущем — механический контакт без
 *    подавления дребезга, и его снимок уходит в CAN как есть. Дребезг на
 *    фронте дал бы пачку пусков и остановов подряд. Команда принимается,
 *    только если продержалась стабильной FAN_CMD_DEBOUNCE_MS — это отсекает
 *    дребезг раньше, чем он дойдёт до силовой части.                        */
#define FAN_CMD_DEBOUNCE_MS  150

/* 2. МИНИМАЛЬНАЯ ПАУЗА ПОСЛЕ РАЗМЫКАНИЯ ОБМОТКИ. Между закрытием нижнего
 *    плеча и повторным пуском должно пройти не меньше FAN_MIN_OFF_MS.
 *    Прежнее обоснование (заряд бутстрепных ёмкостей верхних ключей) к этому
 *    каскаду не относится: верхнее плечо здесь P-канальное и питается от
 *    драйвера TPM27524 напрямую, бутстрепа нет. Пауза сохранена как ещё один
 *    ограничитель темпа пусков — она же задаёт минимальную длительность
 *    разомкнутого состояния, за которую заведомо успевает закончиться любой
 *    переходный процесс в обмотке.                                          */
#define FAN_MIN_OFF_MS       500

/* 3. ОГРАНИЧЕНИЕ ЧАСТОТЫ ПУСКОВ. Даже корректный пуск — это всплеск тока и
 *    порция тепла в ключах. Счётчик пусков «дырявый»: каждый пуск добавляет
 *    1, каждые FAN_CYCLE_DECAY_MS вычитается 1. Набралось FAN_CYCLE_LIMIT —
 *    канал уходит в паузу FAN_CYCLE_COOLDOWN_MS, за которую кристалл ключей
 *    успевает остыть. Редкие штатные включения до лимита не доходят никогда. */
#define FAN_CYCLE_LIMIT        6
#define FAN_CYCLE_DECAY_MS 20000
#define FAN_CYCLE_COOLDOWN_MS 30000

/* Минимальная длительность импульса ШИМ, отсчёты TIM4 (288 отсчётов = 4 мкс
 * при 72 МГц).
 *
 * В начале разгона расчётная скважность даёт импульсы в единицы наносекунд —
 * ключ не успевает открыться и работает в линейном режиме, рассеивая всю
 * мощность на кристалле. Порог привязан к времени переключения: сам переход
 * занимает 1.3..2 мкс (см. расчёт частоты выше), поэтому импульс должен быть
 * как минимум вдвое длиннее перехода, иначе ключ не выходит на полное
 * открытие и импульс состоит из одних фронтов. 4 мкс дают двукратный запас к
 * пессимистичной оценке перехода.
 *
 * Импульсы короче порога не выдаются вовсе (канал остаётся закрытым);
 * симметрично отсекается и «почти полностью открыто». При периоде 18000 порог
 * соответствует 1.6 % скважности — разгон просто стартует с неё, что на
 * неподвижном роторе даёт около 1 А.                                        */
#define FAN_PWM_MIN_PULSE    288

/* ===== Датчики тока вентиляторов (NSM2012-30B5R-DSPR) =====================
 * На плате распаян NSM2012-30B5R-DSPR (а НЕ ACS724, как подписано в схеме):
 * холловский датчик ±30 А, чувствительность 66.67 мВ/А при VCC = 5 В, ноль при
 * нулевом токе = VCC/2, выход РАТИОМЕТРИЧНЫЙ (суффикс R).
 *
 * МАСШТАБ ВЫЧИСЛЯЕТСЯ ИЗ ИЗМЕРЕННОГО НУЛЯ, а не задан константой. Это прямое
 * следствие ратиометричности и избавляет от главной неизвестной — напряжения
 * питания датчика:
 *
 *     Vout  = VCC/2 + S·I,   где S = S5·(VCC/5)
 *     delta/zero = (S·I)/(VCC/2) = 2·S5·I/5
 *     =>  I = (delta/zero) · 5/(2·S5) = (delta/zero) · 37.5 А
 *
 * VCC сократилось, опора АЦП сократилась тоже (входит в delta и zero
 * одинаково). То есть отношение «отсчётов на ампер» к «отсчётам нуля» —
 * чистая константа:
 *
 *     I[мА] = delta · FAN_ADC_FULL_SCALE_MA / zero_adc
 *
 * ЗАЧЕМ ЭТО ЗДЕСЬ. Прежние пороги задавались произведением на
 * ACS724_ADC_PER_AMP = 82 отсчёта/А (ACS724 на 3.3 В). Замер на плате
 * controller-fuel дал ноль 1990 отсчётов = 1.604 В, то есть тракт с делителем
 * и масштабом 53 отсчёта/А: все пороги оказывались в 1.55 раза выше
 * задуманных — «17 А» пиковой защиты означали 26 А, «9 А» перегрузки — 14 А,
 * а «1 А» обрыва нагрузки — 1.6 А. В миллиамперах такой ошибки быть не может.
 *
 * ПОТОЛОК ИЗМЕРЕНИЯ. Выше 4095 отсчётов АЦП не видит ничего, поэтому измеримый
 * ток ограничен оставшимся над нулём запасом:
 *
 *     потолок[мА] = (4095 - zero_adc) · FAN_ADC_FULL_SCALE_MA / zero_adc
 *
 * При нуле 1990 это 39.7 А — пиковый порог проходит свободно. Но при питании
 * датчика 5 В без делителя (ноль ~3102) потолок всего 12 А, и порог 17 А
 * стал бы НЕДОСТИЖИМЫМ. Такую защиту надо не «оставить мёртвой», а честно
 * отключить и показать это в телеметрии (см. FAN_OC_MIN_HEADROOM_MA).       */
#define FAN_ADC_FULL_SCALE_MA 37500 /* 5 В / (2 · 66.67 мВ/А), см. вывод выше */
#define FAN_ZERO_NOMINAL_ADC   2047 /* запасной ноль при неудачной калибровке */

/* Ноль каждого датчика измеряется при старте, пока верхние плечи закрыты
 * (вентиляторы не запитаны) при уже поднятом EN. Ждать «пока мосты
 * обесточены» здесь НЕЛЬЗЯ: снятый EN на этой плате означает полностью
 * открытый верхний ключ, то есть вентилятор на полном ходу (см. ниже).
 *
 * Границы правдоподобия принимают и 2047 (питание датчика 3.3 В), и 3102
 * (5 В), но отвергают оборванный вход (около нуля) и закороченный на питание
 * (у потолка). Прежнее окно 300..3800 принимало и явно оборванный вход, то
 * есть «калибровало» шум и объявляло его достоверным нулём.
 *
 * ЕСЛИ КАЛИБРОВКА НЕ ПРОШЛА, защита по току этого канала ОТКЛЮЧАЕТСЯ, и это
 * сознательная смена прежнего решения («вентилятор без защиты не оставляем»).
 * Причина в том, что при неисправном датчике защита не «остаётся в работе», а
 * работает НАОБОРОТ: показание около нуля читается как отсутствие тока, и
 * контроль обрыва нагрузки через FAN_OPEN_LOAD_MS сам останавливает исправный
 * вентилятор. То есть прежняя формулировка защищала не вентилятор, а от
 * вентилятора. Незащищённый канал виден в телеметрии; настоящее КЗ при этом
 * заканчивается предохранителем.                                            */
#define ACS_CAL_MIN_ADC      1500
#define ACS_CAL_MAX_ADC      3600
#define ACS_CAL_SAMPLES        32

/* --- Темп выборки защиты --------------------------------------------------
 * Главный цикл прокручивается за единицы микросекунд, а АЦП обновляет канал
 * раз в ~224 мкс (полный обход 8 каналов). Если брать ток на каждой итерации
 * цикла, одно и то же значение будет посчитано сотни раз: любой одиночный
 * выброс АЦП «залипал» бы в буфере и набивал счётчики защиты как настоящий
 * переток. Поэтому защита работает по собственному тику 1 мс — к этому
 * моменту АЦП гарантированно обновил канал, и каждая выборка независима.   */
#define FAN_I_SAMPLE_MS        1

/* --- Быстрая защита: короткое замыкание или клин ротора -------------------
 * Порог 17 А выбран ниже насыщения датчика (±30 А) и заметно выше любого
 * рабочего режима: на 50 % скважности вентилятор 130 Вт потребляет ~3..5 А
 * среднего тока, в момент разгона — до ~10 А.
 *
 * ПОРОГ ТОЛЬКО ВЕРХНИЙ. Ток однонаправленный (V_BAT -> верхний ключ ->
 * обмотка -> датчик -> нижний ключ -> земля), поэтому отрицательное отклонение
 * означает не переток, а неисправность измерения. Прежнее сравнение по МОДУЛЮ
 * отклонения гасило исправный вентилятор по оборванному проводу к датчику;
 * с односторонним порогом неисправный датчик его никогда не превысит.
 *
 * СЧЁТ ПРЕВЫШЕНИЙ В ОКНЕ вместо прежнего «дырявого ведра». АЦП не
 * синхронизирован с ШИМ и обходит 8 каналов за ~224 мкс, поэтому выборки
 * садятся в случайные точки пульсации — требовать НЕПРЕРЫВНОГО превышения
 * бессмысленно, и оба метода это учитывают. Окно выбрано ради единообразия с
 * остальными узлами (controller-fuel, ведущий, ведомый) и потому, что оно не
 * опирается на предположение о том, рублено показание ШИМ или нет: датчик
 * стоит в цепи НИЖНЕГО плеча, то есть внутри контура гашения
 * GND -> VD32/33/34 -> обмотка -> датчик -> нижний ключ, и для индуктивной
 * нагрузки показание близко к НЕПРЕРЫВНОМУ току обмотки, а не к рубленому
 * току от бортсети. Следствие: телеметрия отдаёт ток ОБМОТКИ (на рабочей
 * скважности ~3 А), а не ток от бортсети (~1.5 А).
 *
 * 5 превышений за 50 мс при тике 1 мс: реальный переток набирает их за
 * единицы миллисекунд, одиночный выброс АЦП окно не набирает.               */
#define FAN_OC_PEAK_MA     17000   /* 17 А                                   */
#define FAN_OC_WINDOW_MS      50   /* длина окна наблюдения, мс              */
#define FAN_OC_WINDOW_HITS     5   /* превышений в окне для срабатывания     */

/* Сколько запаса измерения должно оставаться НАД пиковым порогом, чтобы
 * защита имела смысл (обоснование и расчёт потолка — выше, у датчика).      */
#define FAN_OC_MIN_HEADROOM_MA 2000

/* --- Медленная защита: длительная перегрузка ------------------------------
 * Средний ток считается экспоненциальным фильтром (сдвиг на FAN_I_AVG_SHIFT
 * — усреднение по 64 выборкам, при тике 1 мс это постоянная времени ~64 мс:
 * заведомо больше периода ШИМ, поэтому пульсация усредняется полностью, и
 * заведомо меньше выдержек защит ниже). Порог 9 А — примерно втрое выше
 * рабочего тока обмотки на 50 % скважности и ниже длительно допустимого
 * тока проводки; выдержка в 2 с пропускает разгон и кратковременное
 * загрязнение крыльчатки, но ловит подклинивание подшипника.                */
#define FAN_I_AVG_SHIFT        6   /* постоянная фильтра среднего тока       */
#define FAN_OC_AVG_MA       9000   /* 9 А                                    */
#define FAN_OC_AVG_TRIP_MS  2000   /* выдержка перегрузки, мс                */

/* --- Обрыв нагрузки (вентилятор отключён/оборван) -------------------------
 * Если канал под рабочей скважностью, а среднего тока практически нет —
 * вентилятора на выходе нет. Тока в цепи тоже нет, поэтому канал гасится
 * сразу (стекать нечему), а факт поднимается в телеметрию: оператор узнаёт о
 * мёртвом вентиляторе до перегрева двигателя.
 * Порог 1 А заведомо ниже тока даже холостого хода (~1.5..2 А).
 *
 * ВАЖНО: этот признак работает только при ДОСТОВЕРНОМ нуле датчика. Неисправный
 * датчик даёт ровно ту же картину «тока нет», и без проверки достоверности
 * останавливал бы исправный вентилятор (см. ACS_CAL_MIN_ADC).               */
#define FAN_OPEN_LOAD_MA    1000   /* 1 А                                    */
#define FAN_OPEN_LOAD_MS    1500   /* выдержка обнаружения обрыва, мс        */

/* --- Поведение после аварии ----------------------------------------------
 * Охлаждение — функция, отказ которой ведёт к перегреву, поэтому авария не
 * защёлкивается намертво: канал сам пробует запуститься через
 * FAN_FAULT_RETRY_MS. Если причина не ушла, попытки прекращаются после
 * FAN_FAULT_RETRY_MAX подряд, и канал остаётся выключенным до снятия команды
 * (оператор отпускает вход deadman) — так неисправный канал не «долбит»
 * бортсеть бесконечными пусками.                                            */
#define FAN_FAULT_RETRY_MS  5000
#define FAN_FAULT_RETRY_MAX    3

/* Индексы датчиков тока в ADS_RES_BUFFER (ADC CH5/CH6 = PA5/PA6). */
#define DRV1_CURRENT_IDX  4
#define DRV2_CURRENT_IDX  5
#define TEMP_SENS_1_IDX   6
#define TEMP_SENS_2_IDX   7

/* ===== Тепловая защита платы ============================================== */
/* Датчики TEMP_SENS_1 (PA7) и TEMP_SENS_2 (PB0) меряют плату — тот самый
 * узел, который греют ключи мостов. Поэтому останов вентиляторов реально
 * снижает температуру: снимается рассеиваемая мощность с ключей.
 *
 * ЧТО СЧИТАТЬ КРИТИЧНЫМ. Ограничивает не припой (SnAgCu плавится при 217 °C),
 * а самое слабое звено на плате:
 *   - электролитические конденсаторы  — 105 °C (ресурс падает вдвое на каждые
 *     +10 °C, при 105 °C он исчерпывается за сотни часов);
 *   - STM32F103 и логика             — 85 °C по паспорту;
 *   - надёжность паяных соединений   — деградация начинается заметно раньше
 *     плавления, примерно от 120 °C длительно.
 * Связывающий предел — 85 °C по МК. Порог отключения взят 95 °C: это уже
 * авария (МК за паспортом), но ещё далеко и от электролитов, и от припоя.
 * Возврат в работу — при 75 °C, гистерезис в 20 °C исключает автоколебания.
 *
 * ПЕРЕСЧЁТ В ОТСЧЁТЫ АЦП. Датчики — NTC 10 кОм B=3950 в нижнем плече делителя
 * с подтяжкой 10 кОм на 3.3 В:
 *     R(T) = 10000 · exp(3950 · (1/T − 1/298.15)),  T в кельвинах
 *     ADC  = 4095 · R / (R + 10000)
 * Отсюда: 25 °C -> 2047; 75 °C -> 532; 85 °C -> 401; 95 °C -> 305; 105 °C -> 234.
 * При таком включении рост температуры УМЕНЬШАЕТ отсчёт — это задаёт
 * TEMP_SENSOR_NTC = 1.
 *
 * ЕСЛИ ДАТЧИКИ КОГДА-НИБУДЬ ЗАМЕНЯТ на линейные с положительным наклоном
 * (LM35/TMP36 и подобные) — поставить TEMP_SENSOR_NTC = 0 и пересчитать два
 * порога; направление сравнений перевернётся само (см. TEMP_HOTTER).        */
#define TEMP_SENSOR_NTC     1    /* 1 — рост температуры уменьшает отсчёт АЦП */
#define TEMP_TRIP_ADC     305    /* ~95 °C — отключение вентиляторов          */
#define TEMP_CLEAR_ADC    532    /* ~75 °C — возврат в работу                 */

/* «Датчик A горячее порога B» с учётом наклона характеристики. */
#if TEMP_SENSOR_NTC
#define TEMP_HOTTER(adc, thr)  ((adc) < (thr))
#else
#define TEMP_HOTTER(adc, thr)  ((adc) > (thr))
#endif

/* Выдержка: перегрев должен подтвердиться, одиночная выборка не отключает.
 * Тепловые процессы медленные, секунда ничего не решает.                    */
#define TEMP_TRIP_MS     1000
#define TEMP_SAMPLE_MS     10    /* тик опроса датчиков температуры          */

/* Границы правдоподобия. Вне их датчик оборван (отсчёт у потолка) или закорочен
 * (отсчёт у нуля) — по такому показанию защита НЕ отключает вентиляторы:
 * остановленное охлаждение опаснее неизвестной температуры. Факт уходит в
 * телеметрию отдельным битом, исправный второй датчик продолжает защищать. */
#define TEMP_VALID_MIN_ADC   30
#define TEMP_VALID_MAX_ADC 4000

/* ===== Защита при потере связи с ведущим ================================== */
/* Ведущий шлёт цикл кадров с тиком TIM1 CH3 (20 мс), период кадра comp
 * ≈ 100 мс. Таймаут 500 мс покрывает ~5 пропусков подряд. Реакция на потерю
 * связи — ШТАТНЫЙ управляемый останов (тот же, что по команде «выключить»),
 * а не обрыв питания: с точки зрения силовой части это одно и то же
 * безопасное действие.                                                      */
#define CAN_CONTROL_TIMEOUT_MS 500

/* ===== Адресация прикладных CAN-сообщений ================================= */
/* StdId = (device_id << 8) | (BASE_* + index).                              */
#define BASE_RELAY      0x00
#define BASE_ADC1       0x10
#define BASE_ADC2       0x20
#define BASE_CONTROL    0x30
#define BASE_COMP       0x40
#define BASE_RELAY_OUT  0x50
#define BASE_AKPP       0x60
#define BASE_DEBUG      0xA0 /* телеметрия вентиляторов; идентификаторы выше
                                рабочих кадров, чтобы проигрывать им арбитраж */

#define RELAY_COUNT     5
#define ADC1_COUNT      2
#define ADC2_COUNT      2
#define CONTROL_COUNT   1
#define COMP_COUNT      1
#define RELAY_OUT_COUNT 1
#define AKPP_COUNT      1

/* ===== Назначение реле узла охлаждения ==================================== */
/* Вентиляторы коммутируются силовыми выходами L1/L2, а не реле, поэтому
 * реле 1-4 остаются свободными (резерв под жалюзи/помпу/сигнализацию).      */
#define RELAY_POWER     4  /* общее реле питания (включается при старте)     */

#ifdef __cplusplus
}
#endif

#endif /* __CONFIG_H */
